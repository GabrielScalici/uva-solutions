#include <iostream>
#include <stack>
#include <string>
#include <algorithm>
#include <cstdlib>
#include <stdio.h>
#include <cstring>

using namespace std;

int main(int argc, char const *argv[]){

  float p;

  while(cin >> p){
    float q, r, s, t, u;
    //pegando as outras variaveis
    cin >> q;
    cin >> r;
    cin >> s;
    cin >> t;
    cin >> u;

    //Fazendo as contas
    

  }


  return 0;
}
